install.packages("signal")
install.packages("tuneR") #for reading .wav files
install.packages("ggplot2") # for plotting 
install.packages("oce") #plot images 
install.packages("seewave") #handling audio data
install.packages("gridExtra") #for formatting ggplot graphs
devtools::install_github("NJITFortune/DFWE") #custom package for plotting spectrogram

library(signal)
library(tuneR)
library(ggplot2)
library(oce)
library(seewave)
library(gridExtra)
library(DFWE)

<pre class="r"><code>library(signal)</code>
  <code>library(tuneR)</code>
  <code>library(ggplot2)</code>
  <code>library(oce)</code>
  <code>library(seewave)</code>
  <code>library(gridExtra)</code>
  <code>library(DFWE)</code></pre>